# DEPRECATED: production Prisma schema is now prisma/schema.prisma (PostgreSQL).
# This folder is kept only as a historical mirror of the first PG migration.
# Prefer: prisma/schema.prisma + prisma/migrations/
#
# For SQLite → Postgres one-time import tooling see:
#   prisma/sqlite-legacy/
#   scripts/migrate-sqlite-to-postgres.ts
